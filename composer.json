{
    "name": "mockpix/mock-pix-v2",
    "description": "Mock funcional PIX em PHP 8.0+ com padrão MVC",
    "type": "project",
    "require": {
        "php": "^8.0",
        "ext-pdo": "*",
        "ext-json": "*",
        "ext-curl": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "migrate": "php scripts/migrate.php",
        "worker": "php scripts/worker.php",
        "report": "php scripts/report_daily.php"
    }
}
